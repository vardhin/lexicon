[project]
name = "lexicon-shell"
version = "0.1.0"
description = "Lexicon Shell Microservice — PTY-based shell session manager"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "websockets>=15.0",
]

[project.scripts]
lexicon-shell = "shell_server:main"
