{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "whatsapp-organ",
  "description": "WhatsApp organ runs as a separate process — no special webview capabilities needed.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
